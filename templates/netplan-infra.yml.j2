network:
  version: 2
  ethernets:
    {{ lan_interface }}:
      addresses:
        - {{ infra_ip }}/24
      routes:
        - to: default
          via: {{ gateway }}
      nameservers:
        addresses:
          - {{ dns_primary_ip }}
          - {{ dns_secondary_ip }}
          - 1.1.1.1
        search:
          - {{ domain }}
